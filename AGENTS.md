# s1-line-guard — 助手纪律（本仓库）

本文件会被会话上下文加载。改本仓库代码时**必须**遵守。

## 行为规格优先

1. **唯一业务真相源：** `docs/behavior-spec.md`  
2. 改 `onboard/line_guard.py`（或其它实现）前：先读 `behavior-spec.md` 对应章节。  
3. **禁止**在实现里加入规格未写明的限制或「额外能力」，例如：  
   - 擅自冷却、墙钟超时、空扫次数上限、coast、加大 miss 放弃阈值等  
4. 若认为需要新限制/新能力：  
   - **先**向用户说明原因与方案并等待确认；  
   - **再**改 `docs/behavior-spec.md`；  
   - **最后**才改代码。  
5. 实现与规格冲突时：**以规格为准**，删掉多余逻辑。

## 文档边界

| 文件 | 写什么 | 不写什么 |
|------|--------|----------|
| `docs/behavior-spec.md` | 已定行为（短、可对照） | API 长文、灯光细节、过程流水 |
| `docs/design-notes.md` | API 坑、灯光含义、实现备忘 | 另立一套状态机 |
| `docs/dev-plan.md` | 交付清单、联调顺序、范围外 | 与代码不一致的说明 |
| `README.md`、`onboard/README.md` | 用法与状态机摘要（与代码一致） | 过程记录、对照旧方案的说明 |
| `onboard/line_guard.py` 头注释 | 指向 behavior-spec + 极短摘要 | 与规格矛盾的流程 |

## 只保留现行内容

1. 仓库里只写**现在**的行为、原则与实现；不要保留「曾经做过 / 已删掉什么」的清单或对照表。  
2. 无用代码、无用文件、过时文档段落：**直接删除**，不必写「废弃说明」。  
3. **改行为或删状态/参数时**，同一任务内同步所有相关文档与代码，保持一致。  
4. 不要等用户指出文档过时。

## 改代码检查（每次）

- [ ] 新常量/分支是否在 `behavior-spec.md` 有对应条文？  
- [ ] 是否出现规格未要求的「保护性」超时/冷却/计数上限？有则删除或先问用户。  
- [ ] CONFIG 注释与规格用词一致（hit≥3、射3s/停3s 等）。  
- [ ] README / design-notes / dev-plan / onboard README 是否与本次一致？  
- [ ] 是否留下「已废弃 / 历史方案」类文字？有则删掉。  

## 其它

- 用户说「不要动代码」则只分析/改文档（若其要求改文档）。  
- 用户说「提交」：按全局 Git 约定 commit + push。  
