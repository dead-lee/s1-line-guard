# S1 Line Guard（沿线哨兵）

基于 **DJI RoboMaster S1** 官方 App 实验室（车载 Python）的沿线巡逻卫兵程序。

- **Mac / 本仓库**：写需求、维护车载兼容代码骨架、资源与文档  
- **S1 上**：粘贴 `onboard/` 程序后全自动运行  
- **不需要** Mac 与 S1 实时传数据；功能闭环全部在车上完成  

> 当前阶段：需求与骨架就绪，**尚未实现车载控制代码**。

仓库地址：<https://github.com/dead-lee/s1-line-guard>

---

## 1. 项目目标

S1 沿地面 **蓝色** 色带巡逻；在定时哨位抬头扫描是否有人。若识别到行人：

1. 枪口（云台）指向人员  
2. **红色灯光闪烁**  
3. 播放警告（见 `resources/`）  
4. 人员若持续存在超过 **3 秒**，之后 **每隔 1 秒点射一次** 示警  
5. 人员离开视野后，停止射击与告警，恢复巡线巡逻  

一句话：**沿线巡逻的卫兵，哨位扫视；发现入侵则警告，超时点射，人走归队。**

---

## 2. 已确认的产品规格（冻结）

| 项目 | 规格 |
|---|---|
| 巡线颜色 | **蓝色**胶带（官方线识别支持红/绿/蓝；**不支持黑线**） |
| 线宽建议 | 约 15–25 mm（官方推荐量级） |
| 巡逻方式 | 循线前进一段时间 → **停车** → 云台扫描 → 无事则继续循线 |
| 扫描 | **定时 / 分时 scan**（不要求边走边连续扫视） |
| 入侵判定 | **是否识别到行人**（不使用精确 1 m 测距） |
| 警告灯效 | **红色闪烁** |
| 警告语音 | 终结者风格合成语音，文案见下节 |
| 警告动作 | **不需要**点头等额外云台表演 |
| 锁定后满 3 s | 若人员仍在，开始点射 |
| 点射节奏 | **每隔 1 秒点射 1 次**，直到人员离开 |
| 人员离开 | 连续一段时间检测不到人 → 停火、停闪、恢复巡线 |
| 运行环境 | S1 App 实验室 Python（车载） |
| 开发方式 | 在 Mac 编写 `onboard/` 兼容代码 → 拷贝进 App 运行 |

### 2.1 警告文案与语音

中文 edge-tts 可用音色有限，试听后中文机械感/自然度不理想，**主版本改为英文**（更贴近「终结者 / 安保广播」质感）。

**英文（主文件，默认）**

```
Intruder detected. Leave the area immediately. Or you will be fired upon. Consequences will be severe.
```

**中文（语义对照，车上实现仍可用内置音；不强制播放中文 TTS）**

```
发现入侵，请立刻离开，否则开火，后果自负。
```

合成音频：

| 文件 | 说明 |
|---|---|
| `resources/warning_intruder.mp3` | **主警告**：`en-US-ChristopherNeural`，偏慢、偏低、权威播报感 |
| `resources/warning_intruder_alt_guy.mp3` | 备选：`en-US-GuyNeural`，可 A/B 试听 |
| `resources/warning_intruder.vtt` | 主文件字幕时间轴 |

试听：

```bash
afplay resources/warning_intruder.mp3
# 备选
afplay resources/warning_intruder_alt_guy.mp3
```

重新生成：

```bash
./scripts/generate_warning_audio.sh
```

### 2.2 关于车载播放 MP3 的说明

S1 实验室 Python **通常只能调用内置音效 API**，**不能保证**能直接播放自定义 `mp3`。

因此本仓库中的 MP3 定位为：

1. 需求与演示素材（听效果、剪辑、对照文案）  
2. 若后续发现可注入/可播放路径，再接入  
3. **实现第一版时**：优先用 **内置警示音 + 红灯闪烁** 满足「警告」；语音文件保留在 `resources/`

---

## 3. 行为状态机（实现时遵循）

```
                 ┌──────────────┐
                 │    INIT      │  使能线识别 / 行人识别，云台巡线俯角
                 └──────┬───────┘
                        ▼
                 ┌──────────────┐
          ┌─────►│   PATROL     │  低头循蓝线前进 T_move
          │      └──────┬───────┘
          │             │ 时间到 / 到达哨位
          │             ▼
          │      ┌──────────────┐
          │      │    SCAN      │  停车；抬头；yaw 左右扫 T_scan
          │      └──────┬───────┘
          │        无人 │        有人
          │             │          │
          │             │          ▼
          │             │   ┌──────────────┐
          │             │   │    LOCK      │  停车；枪口指向人；红灯闪；播警告
          │             │   └──────┬───────┘
          │             │          │ 满 3s 且人仍在
          │             │          ▼
          │             │   ┌──────────────┐
          │             │   │    FIRE      │  每隔 1s 点射 1 次；保持锁定与红闪
          │             │   └──────┬───────┘
          │             │          │ 连续 T_clear 检测不到人
          │             │          ▼
          │             │   ┌──────────────┐
          │             └───│  RECOVER     │  停火；关红闪；云台回巡线角；找线
          │                 └──────┬───────┘
          │                        │
          └────────────────────────┘
```

### 3.1 建议默认可调参数（实现时放入 config）

| 参数 | 建议初值 | 含义 |
|---|---|---|
| `T_move` | 2.0 s | 每次低头循线时长 |
| `T_scan` | 4.0 s | 单次哨位扫描总时长 |
| `YAW_SCAN_DEG` | ±60°～±90° | 扫描左右幅度 |
| `PITCH_LINE` | 俯视（按实车调） | 巡线云台俯仰 |
| `PITCH_SCAN` | 平视/略仰 | 扫人云台俯仰 |
| `T_WARN_BEFORE_FIRE` | 3.0 s | 发现人后首次点射前等待 |
| `T_FIRE_INTERVAL` | 1.0 s | 点射间隔 |
| `T_clear` | 2.0 s | 连续无人多久算离开 |
| `LINE_COLOR` | blue | 蓝色线 |

---

## 4. 技术边界（已知，实现时遵守）

| 点 | 说明 |
|---|---|
| 单摄像头 | 巡线要低头，扫人要抬头 → **必须分时**，不能可靠「边走边连续扫」 |
| 无精确 1 m | 行人 API 无可靠测距 → 用 **检测到人 = 入侵** |
| 线颜色 | 仅红/绿/蓝；本项目固定 **蓝** |
| 车载 Python | 沙箱 + 官方 `rm_*` API；无 pip、无完整多线程模型 |
| 并发模型 | 用 **状态机 + 事件/轮询**，不用 `threading` 作为主方案 |
| 安全 | 水弹、勿对人面部；演示限定区域与弹药 |

官方背景文档可参考：

- [RoboMaster 开发者文档](https://robomaster-dev.readthedocs.io/zh-cn/latest/)  
- [S1 Programming Guide](https://www.dji.com/robomaster-s1/programming-guide)  

本项目 **不依赖** 外部 `robomaster` PC SDK / S1 解锁。

---

## 5. 仓库结构

```text
s1-line-guard/
├── README.md                 # 本文件：需求与规格
├── LICENSE
├── .gitignore
├── resources/
│   ├── warning_intruder.mp3          # 主警告（英文，权威男声）
│   ├── warning_intruder_alt_guy.mp3  # 备选音色
│   └── warning_intruder.vtt
├── docs/
│   └── design-notes.md       # 设计备忘（状态机与 API 映射草稿）
├── onboard/                  # ★ 将来拷进 S1 App 的代码（现为占位）
│   ├── README.md
│   ├── config.py             # 参数占位
│   └── main.py               # 入口占位（未实现逻辑）
└── scripts/
    └── generate_warning_audio.sh
```

---

## 6. 开发与部署流程（实现阶段）

1. 在 Mac 上只改 `onboard/`，保持 **车载兼容**（无第三方库、无 threading 依赖）。  
2. 用 Git 管理本仓库。  
3. 将 `onboard/main.py`（及拆分模块若有）内容粘贴到 RoboMaster App → 实验室 → Python。  
4. S1 连接 App，场地铺好蓝线，空旷处安排测试人员。  
5. 先跑「仅 PATROL / 仅 SCAN / 仅 LOCK」分阶段联调，再串状态机。

### 6.1 场地建议

- 浅色地面 + 蓝色闭环或往返色带  
- 光线均匀，减少强逆光  
- 测试人员正面站入扫描视野，便于行人识别  

### 6.2 安全清单

- [ ] 水弹已装妥且弹道前方无人近距离面部  
- [ ] 首次联调可先 **禁用开火**（只红灯+语音/内置音）  
- [ ] 确认紧急停机方式（App 停止 / 电源）  

---

## 7. 实现里程碑（后续）

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 需求冻结、仓库骨架、警告 MP3 | **完成** |
| M1 | `onboard`：PATROL + SCAN 分时循环 | 未开始 |
| M2 | LOCK：指向行人 + 红灯闪 + 警告 | 未开始 |
| M3 | FIRE：3s 后每 1s 点射；离开恢复 | 未开始 |
| M4 | 丢线恢复、碰撞处理、参数固化 | 未开始 |
| M5 | 实车演示录像与调参记录 | 未开始 |

---

## 8. 许可

见 [LICENSE](./LICENSE)（MIT）。与 DJI / RoboMaster 商标及官方 SDK 许可无关；硬件与官方 App 使用请遵守大疆条款。
